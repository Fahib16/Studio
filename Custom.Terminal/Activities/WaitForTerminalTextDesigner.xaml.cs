namespace Custom.Terminal.Design
{
    /// <summary>
    /// Code-behind minimal untuk Wait For Terminal Text -- cuma header berwarna, semua
    /// property (Session, Key/Text/Row/Column, dst) diedit lewat Properties
    /// panel standar (sudah tersusun rapi lewat [Category] di code activity-nya).
    /// </summary>
    public partial class WaitForTerminalTextDesigner
    {
        public WaitForTerminalTextDesigner()
        {
            InitializeComponent();
        }
    }
}
